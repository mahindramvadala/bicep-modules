name: Publish Virtual Network Bicep module

on:
    workflow_dispatch:
    pull_request:
        branches:
            - main
        paths:
            - 'VirtualNetwork/**'
            - 'utilities.bicep'
            - '.github/workflows/virtual-network.yml'
    push:
        branches:
            - main
        paths:
            - 'VirtualNetwork/**'
            - 'utilities.bicep'
            - '.github/workflows/virtual-network.yml'
jobs:
    key-vault:
        permissions:
            contents: read
            id-token: write
        uses: ./.github/workflows/bicep-publish.yml
        secrets:
            azure-client-id: ${{ secrets.ClIENT_ID }}
            azure-tenant-id: ${{ secrets.TENANT_ID }}
            azure-subscription-id: ${{ secrets.SUBSCRIPTION_ID }}
        with:
            bicep-file-path: ./VirtualNetwork/
            registry-name: crmahi
            registry-path: network/vnet:1.0
